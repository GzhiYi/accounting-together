<nav bgColor="rgb(255, 232, 59)" class="nav-instance" showIcons="{{['back']}}"></nav>
<van-panel style="margin-bottom: 20rpx;" wx:for="{{logs}}" wx:key="{{item._id}}" title="{{item.version}}">
  <view class='update-content' wx:for="{{item.content}}" wx:key="{{index}}">
    {{item}}
  </view>
</van-panel>
